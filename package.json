{
  "name": "event-network-generator",
  "version": "1.0.0",
  "description": "Event Network Generator",
  "scripts": {
    "dev": "pip install -r requirements.txt && streamlit run event_net_gen.py --server.port 3000 --server.address 0.0.0.0",
    "build": "echo 'No build step required'",
    "start": "pip install -r requirements.txt && streamlit run event_net_gen.py --server.port 3000 --server.address 0.0.0.0",
    "lint": "echo 'No lint step required'"
  },
  "dependencies": {}
}
